#include <stdio.h>
#include <ctype.h>
/**
 * _islower - checks if a char is lowercase
 *
 * @c: only member
 *
 * Return: 1 if lowercase and 0 otherwise
 */
int _islower(char c)
{
	if (islower > 0)
	{
		return (1);
	}
	return (0);
}
