#include <stdio.h>
/**
 * print_line - Prints numbers, from 0 to 14 ten times, followed by a new line
 *
 * @n: length of line
 */
void print_line(int n)
{
	int i;
	
	if (n > 0)
	{
		for (i = 0; i < c; i++)
		{
			putchar('_');
		}
	}
	putchar('\n');
}
